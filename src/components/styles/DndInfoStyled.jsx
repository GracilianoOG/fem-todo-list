import styled from "styled-components";

export const DndInfoStyled = styled.p`
  color: ${({ theme }) => theme.colors.listText};
  font-size: 0.856rem;
  margin-bottom: 5rem;
  text-align: center;
`;
